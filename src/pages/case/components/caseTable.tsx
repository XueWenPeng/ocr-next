import React from 'react';
import { Table } from 'mishu-ui';
import { columns } from '../constants';

const CaseTable = () => {
  return <Table columns={columns} dataSource={[]} />;
};

export default CaseTable;
